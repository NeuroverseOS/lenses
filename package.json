{
  "name": "@neuroverse/lenses",
  "version": "1.0.0",
  "private": true,
  "description": "Lenses — Pick who you want in your corner. A governed AI companion for MentraOS smart glasses.",
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun --watch src/server.ts",
    "demo": "bun src/demo.ts",
    "demo:live": "bun src/demo-live.ts",
    "validate": "bun src/validate.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@mentra/sdk": "3.0.0-hono.8",
    "@neuroverseos/governance": "^0.4.0",
    "@anthropic-ai/sdk": "^0.39.0",
    "openai": "^4.73.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.3.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
